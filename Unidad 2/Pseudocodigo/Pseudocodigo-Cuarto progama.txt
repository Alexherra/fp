# Pseucodigo 4
## Programa que te pida nombre, y apellidos por separado, ademas de imprimirlos en un renglon
### Alejandro Herrera Ramos
### **ITPA**
### **Ingenieria en TICS**

VAR
    CARACTER(Nombre);
    CARACTER(Apellidos);

Array Nombre-Apellido
    NombreCompleto -> [Nombre, Apellido];
    Caracter NmobreCompleto;
    NombreCompleto -> Nombre, Apellidos;

INICIO
    ESCRIBIR ("Escribir nombre");
    LEER(Nombre);
    ESCRIBIR ("Escribir apellidos");
    LEER (Apellidos);
    Escribir(NombreCompleto);

IF
IF Escribir
    print (Escribir);
    print (NombreCompleto);

FIN
